{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:app": "webpack --config webpack.config.dev.js --colors --progress",
    "dev:app": "webpack --config webpack.config.dev.js --colors --progress --watch",
    "dev:server": "webpack-dev-server --config webpack.config.dev.js --open",
    "rollup:app": "rollup -c rollup.config.js -w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  }
}
